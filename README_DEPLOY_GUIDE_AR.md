# دليل النشر الشامل لمنصة رابِط للموارد البشرية

## 📋 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المتطلبات الأساسية](#المتطلبات-الأساسية)
3. [إعداد البيئة](#إعداد-البيئة)
4. [النشر على Vercel](#النشر-على-vercel)
5. [النشر باستخدام Docker](#النشر-باستخدام-docker)
6. [إعداد قاعدة البيانات](#إعداد-قاعدة-البيانات)
7. [أفضل ممارسات الأمان](#أفضل-ممارسات-الأمان)
8. [حل المشاكل](#حل-المشاكل)
9. [الصيانة](#الصيانة)

---

## 🎯 نظرة عامة

رابِط هي منصة شاملة لإدارة الموارد البشرية مبنية باستخدام أحدث التقنيات:

- **الواجهة الأمامية**: React 19 + Vite
- **الواجهة الخلفية**: Node.js + Express + tRPC
- **قاعدة البيانات**: MySQL (متوافقة مع TiDB، Railway، PlanetScale)
- **ORM**: Drizzle ORM
- **مدير الحزم**: pnpm 10.4.1

---

## 📦 المتطلبات الأساسية

### الأدوات المطلوبة

- Node.js 18.x أو أحدث
- pnpm 10.4.1
- Git
- قاعدة بيانات MySQL (محلية أو سحابية)

### الأدوات الاختيارية

- Docker و Docker Compose
- Vercel CLI
- Redis (للتخزين المؤقت)

### أوامر التثبيت

```bash
# تثبيت Node.js (إن لم يكن مثبتاً)
# زيارة: https://nodejs.org/

# تثبيت pnpm
npm install -g pnpm@10.4.1

# التحقق من التثبيت
node --version  # يجب أن يكون v18.x أو أحدث
pnpm --version  # يجب أن يكون 10.4.1
```

---

## 🔧 إعداد البيئة

### 1. استنساخ المستودع

```bash
git clone https://github.com/zeroos889-svg/RabitHR.git
cd RabitHR
```

### 2. تثبيت التبعيات

```bash
pnpm install
```

### 3. إعداد متغيرات البيئة

```bash
# نسخ ملف المثال
cp .env.example .env

# تحرير الملف بالقيم الفعلية
nano .env
```

### المتغيرات المطلوبة

#### قاعدة البيانات (مطلوب)

```env
DATABASE_URL=******host:port/database
```

#### الأمان (مطلوب)

```env
JWT_SECRET=مفتاح-سري-قوي-32-حرف-على-الأقل
SESSION_SECRET=مفتاح-سري-للجلسة
```

#### التطبيق (مطلوب)

```env
NODE_ENV=production
VITE_APP_URL=https://your-domain.com
VITE_APP_TITLE=رابِط - منصة إدارة الموارد البشرية
```

#### الخدمات الاختيارية

```env
# التخزين المؤقت Redis
REDIS_URL=redis://host:port

# خدمة البريد الإلكتروني (Resend)
RESEND_API_KEY=مفتاح_api_الخاص_بك
RESEND_FROM_EMAIL=noreply@yourdomain.com

# تخزين AWS S3
AWS_ACCESS_KEY_ID=مفتاح_الوصول
AWS_SECRET_ACCESS_KEY=المفتاح_السري
AWS_REGION=us-east-1
AWS_S3_BUCKET=اسم-الحاوية

# بوابات الدفع
MOYASAR_API_KEY=مفتاح_ميسر
TAP_API_KEY=مفتاح_تاب

# تتبع الأخطاء Sentry
VITE_SENTRY_DSN=رابط_sentry_dsn
```

### 4. توليد المفاتيح السرية

```bash
# توليد JWT_SECRET
openssl rand -base64 32

# توليد SESSION_SECRET
openssl rand -base64 32
```

---

## 🚀 النشر على Vercel

### الطريقة 1: النشر عبر GitHub (موصى به)

#### الخطوة 1: رفع الكود إلى GitHub

```bash
git add .
git commit -m "feat: تحضير للنشر"
git push origin main
```

#### الخطوة 2: الربط مع Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. انقر "Add New Project"
3. استورد مستودع GitHub الخاص بك
4. اختر مشروع RabitHR

#### الخطوة 3: إعدادات البناء

سيكتشف Vercel الإعدادات تلقائياً من `vercel.json`:

- **الإطار**: Vite
- **أمر البناء**: `pnpm build`
- **أمر التثبيت**: `pnpm install`
- **مجلد الإخراج**: `dist`

#### الخطوة 4: إضافة المتغيرات البيئية

في لوحة تحكم Vercel:

1. اذهب إلى **Settings** → **Environment Variables**
2. أضف جميع المتغيرات المطلوبة من `.env.example`
3. لكل متغير:
   - **المفتاح**: اسم المتغير (مثل `DATABASE_URL`)
   - **القيمة**: القيمة الفعلية
   - **البيئات**: اختر Production، Preview، Development

**المتغيرات الحرجة المطلوبة:**

```
NODE_ENV=production
DATABASE_URL=******host:port/db
JWT_SECRET=المفتاح-السري-المولد
SESSION_SECRET=المفتاح-السري-المولد
VITE_APP_URL=https://your-domain.vercel.app
```

#### الخطوة 5: النشر

انقر **"Deploy"** وانتظر اكتمال البناء.

### الطريقة 2: النشر عبر Vercel CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول إلى Vercel
vercel login

# ربط المشروع
vercel link

# سحب المتغيرات البيئية (إذا كانت معدة سابقاً)
vercel env pull .env.local

# النشر للمعاينة
vercel

# النشر للإنتاج
vercel --prod
```

---

## 🐳 النشر باستخدام Docker

### المتطلبات

- Docker مثبت
- Docker Compose مثبت

### استخدام Docker Compose (موصى به)

#### 1. تحديث docker-compose.yml

تأكد من أن `docker-compose.yml` يحتوي على متغيرات البيئة الصحيحة:

```yaml
version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=******mysql:3306/rabithr
      - JWT_SECRET=${JWT_SECRET}
      - SESSION_SECRET=${SESSION_SECRET}
    depends_on:
      - mysql

  mysql:
    image: mysql:8
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=rabithr
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
```

#### 2. البناء والتشغيل

```bash
# بناء الصورة
docker-compose build

# تشغيل الخدمات
docker-compose up -d

# عرض السجلات
docker-compose logs -f app

# إيقاف الخدمات
docker-compose down
```

### استخدام Docker فقط

#### 1. بناء الصورة

```bash
docker build -t rabithr:latest .
```

#### 2. تشغيل الحاوية

```bash
docker run -d \
  --name rabithr \
  -p 3000:3000 \
  -e NODE_ENV=production \
  -e DATABASE_URL=******host:3306/db \
  -e JWT_SECRET=المفتاح-السري \
  -e SESSION_SECRET=المفتاح-السري \
  rabithr:latest
```

#### 3. التحقق

```bash
# التحقق من تشغيل الحاوية
docker ps

# عرض السجلات
docker logs rabithr

# الوصول إلى shell
docker exec -it rabithr sh
```

---

## 💾 إعداد قاعدة البيانات

### الخيار 1: Railway MySQL

1. إنشاء حساب Railway: [railway.app](https://railway.app)
2. إنشاء قاعدة بيانات MySQL جديدة
3. نسخ نص الاتصال من لوحة التحكم Railway
4. إضافته إلى متغيرات البيئة:
   ```env
   DATABASE_URL=******host.railway.app:port/railway
   ```

### الخيار 2: TiDB Cloud

1. إنشاء حساب TiDB Cloud: [tidbcloud.com](https://tidbcloud.com)
2. إنشاء cluster جديد
3. الحصول على نص الاتصال
4. إضافته إلى متغيرات البيئة:
   ```env
   DATABASE_URL=******host.tidbcloud.com:4000/database
   ```

### الخيار 3: MySQL محلي

```bash
# تثبيت MySQL
# macOS
brew install mysql

# Ubuntu
sudo apt install mysql-server

# بدء MySQL
mysql -u root -p

# إنشاء قاعدة البيانات
CREATE DATABASE rabithr_prod;
CREATE USER 'rabithr'@'localhost' IDENTIFIED BY 'كلمة_مرور_آمنة';
GRANT ALL PRIVILEGES ON rabithr_prod.* TO 'rabithr'@'localhost';
FLUSH PRIVILEGES;
```

### تشغيل الترحيلات

```bash
# دفع المخطط إلى قاعدة البيانات
pnpm db:push

# أو تشغيل الترحيلات يدوياً
pnpm drizzle-kit generate
pnpm drizzle-kit migrate
```

---

## 🔒 أفضل ممارسات الأمان

### 1. إدارة الأسرار

**افعل:**

- ✅ خزّن الأسرار في متغيرات البيئة
- ✅ استخدم `.env` للتطوير المحلي
- ✅ استخدم متغيرات بيئة Vercel للإنتاج
- ✅ دوّر الأسرار بانتظام (كل 90 يوم)
- ✅ استخدم أسرار قوية مولدة عشوائياً

**لا تفعل:**

- ❌ ترفع `.env` إلى Git
- ❌ تشارك الأسرار في الكود أو التعليقات
- ❌ تستخدم أسرار ضعيفة أو متوقعة
- ❌ تضع بيانات الاعتماد مباشرة في الكود

### 2. أمان قاعدة البيانات

- فعّل SSL/TLS لاتصالات قاعدة البيانات
- استخدم كلمات مرور قوية
- قائمة بيضاء لعناوين IP الضرورية فقط
- نسخ احتياطية منتظمة
- مراقبة النشاط المشبوه

### 3. أمان التطبيق

- حافظ على تحديث التبعيات: `pnpm update`
- قم بفحوصات أمنية: `pnpm audit`
- استخدم HTTPS فقط
- فعّل CORS بشكل صحيح
- نفّذ تحديد المعدل (Rate Limiting)
- استخدم رؤوس الأمان (مُعدّة بالفعل في `vercel.json`)

### 4. التحكم بالوصول

- استخدم كلمات مرور قوية للمسؤولين
- نفّذ المصادقة الثنائية للحسابات الإدارية
- فحوصات أمنية منتظمة
- مبدأ الامتياز الأدنى

---

## 🔍 حل المشاكل

### فشل البناء

#### المشكلة: "Module not found"

```bash
# مسح الذاكرة المؤقتة وإعادة التثبيت
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

#### المشكلة: "أخطاء TypeScript"

```bash
# التحقق من الأخطاء
pnpm check

# إصلاح تلقائي إن أمكن
pnpm format
```

### مشاكل الاتصال بقاعدة البيانات

#### المشكلة: "ETIMEDOUT" أو "ECONNREFUSED"

- تحقق من صحة DATABASE_URL
- تحقق من تشغيل خادم قاعدة البيانات
- تحقق من أن قائمة عناوين IP تتضمن خادمك
- تحقق من إعدادات الجدار الناري

#### المشكلة: "Access denied"

- تحقق من اسم المستخدم وكلمة المرور
- تحقق من أن المستخدم لديه الصلاحيات الصحيحة
- تأكد من صحة إعدادات SSL

### مشاكل النشر على Vercel

#### المشكلة: "env should be object"

- **الحل**: تم إصلاحه في `vercel.json`
- تأكد من إعداد متغيرات البيئة في لوحة تحكم Vercel
- لا تضعها في ملف `vercel.json`

#### المشكلة: "Build timeout"

- ترقية إلى Vercel Pro لأوقات بناء أطول
- تحسين عملية البناء
- تفعيل التخزين المؤقت

### مشاكل Docker

#### المشكلة: "ENOENT: no such file or directory, open '/app/patches/...'"

- **الحل**: تم إصلاحه بالفعل في Dockerfile
- تأكد من `COPY patches ./patches` قبل `pnpm install`

#### المشكلة: "Network error during pnpm install"

- تحقق من اتصال الإنترنت
- جرب استخدام mirror لـ npm registry
- زد مهلة بناء Docker

---

## 🔄 الصيانة

### المهام المنتظمة

#### يومياً

- مراقبة سجلات التطبيق
- التحقق من معدلات الأخطاء
- التحقق من نجاح النسخ الاحتياطي

#### أسبوعياً

- مراجعة تنبيهات الأمان
- التحقق من أداء قاعدة البيانات
- تحديث التبعيات (عند الحاجة)

#### شهرياً

- فحص أمني
- تحسين الأداء
- تنظيف قاعدة البيانات
- مراجعة وتدوير الأسرار

### تحديث التطبيق

```bash
# سحب آخر التغييرات
git pull origin main

# تثبيت التبعيات الجديدة
pnpm install

# تشغيل الترحيلات
pnpm db:push

# البناء
pnpm build

# الاختبار
pnpm test

# النشر
vercel --prod
```

### النسخ الاحتياطي لقاعدة البيانات

```bash
# نسخ احتياطي MySQL
mysqldump -u username -p database_name > backup_$(date +%Y%m%d).sql

# استعادة
mysql -u username -p database_name < backup_20250105.sql
```

### مراقبة التطبيق

#### استخدام لوحة تحكم Vercel

1. اذهب إلى مشروعك في Vercel
2. انقر "Analytics"
3. راقب:
   - عدد الطلبات
   - معدل الأخطاء
   - وقت الاستجابة
   - معدل إصابة الذاكرة المؤقتة

#### استخدام Sentry (إذا تم التكوين)

1. تحقق من تقارير الأخطاء
2. راقب الأداء
3. اضبط التنبيهات

---

## 📞 الدعم والموارد

### الوثائق

- [README الرئيسي](./README.md)
- [دليل النشر على Vercel (عربي)](./VERCEL_DEPLOYMENT_AR.md)
- [دليل النشر على Vercel (إنجليزي)](./VERCEL_DEPLOYMENT_EN.md)
- [دليل حل المشاكل](./VERCEL_TROUBLESHOOTING_EN.md)
- [مراجعة الأمان](./SECURITY_REVIEW.md)

### موارد خارجية

- [وثائق Vercel](https://vercel.com/docs)
- [وثائق Drizzle ORM](https://orm.drizzle.team/)
- [وثائق React](https://react.dev/)
- [وثائق Node.js](https://nodejs.org/docs/)

### المجتمع

- GitHub Issues: للإبلاغ عن الأخطاء وطلب الميزات
- GitHub Discussions: لطرح الأسئلة ومشاركة الأفكار

---

## ✅ قائمة التحقق من النشر

قبل النشر، تأكد من:

### قبل النشر

- [ ] جميع متغيرات البيئة معدّة
- [ ] قاعدة البيانات معدّة ويمكن الوصول إليها
- [ ] الأسرار قوية وآمنة
- [ ] `.env` موجود في `.gitignore`
- [ ] جميع الاختبارات تنجح: `pnpm test`
- [ ] البناء ينجح: `pnpm build`
- [ ] فحص TypeScript ينجح: `pnpm check`

### النشر

- [ ] الكود مرفوع إلى GitHub
- [ ] مشروع Vercel مربوط
- [ ] متغيرات البيئة مضافة في Vercel
- [ ] ترحيلات قاعدة البيانات تم تشغيلها
- [ ] النشر الأولي ناجح

### ما بعد النشر

- [ ] التطبيق متاح على رابط الإنتاج
- [ ] اتصال قاعدة البيانات يعمل
- [ ] المصادقة تعمل
- [ ] الميزات الأساسية مختبرة
- [ ] SSL/HTTPS مفعّل
- [ ] المراقبة معدّة
- [ ] النسخ الاحتياطي معدّ
- [ ] الوثائق محدثة

---

## 🎉 نجح!

منصة رابِط للموارد البشرية الآن منشورة وجاهزة للاستخدام!

**رابط الإنتاج**: `https://your-project.vercel.app`

**الخطوات التالية**:

1. اختبر جميع الميزات بدقة
2. اضبط المراقبة والتنبيهات
3. عدّ النسخ الاحتياطية المنتظمة
4. درّب فريقك
5. ابدأ باستخدام المنصة!

---

**الإصدار**: 1.0.0  
**آخر تحديث**: 2025-11-05  
**المسؤول**: فريق رابِط
