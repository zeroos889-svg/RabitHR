# 🚀 دليل نشر منصة رابِط على Vercel

## 📋 نظرة عامة

هذا الدليل يشرح بالتفصيل كيفية نشر منصة **رابِط** لإدارة الموارد البشرية على منصة **Vercel** مع جميع الإعدادات المطلوبة.

---

## ✅ المتطلبات الأساسية

قبل البدء، تأكد من توفر:

1. **حساب Vercel** - سجّل مجاناً على [vercel.com](https://vercel.com)
2. **حساب GitHub** - يجب أن يكون المشروع على GitHub
3. **قاعدة بيانات MySQL** - يُنصح باستخدام:
   - [Railway](https://railway.app) (مجاني حتى $5/شهر)
   - [PlanetScale](https://planetscale.com) (خطة مجانية متاحة)
   - [TiDB Cloud](https://tidbcloud.com) (خطة مجانية متاحة)
4. **حساب Redis** (اختياري) - للتخزين المؤقت وتحسين الأداء

---

## 📝 الخطوات التفصيلية

### 1️⃣ تجهيز قاعدة البيانات

#### أ. إنشاء قاعدة بيانات MySQL على Railway:

1. سجّل دخول على [railway.app](https://railway.app)
2. انقر على **New Project**
3. اختر **Provision MySQL**
4. انسخ رابط الاتصال بقاعدة البيانات (`DATABASE_URL`)
   ```
   mysql://root:password@containers-us-west-xxx.railway.app:3306/railway
   ```

#### ب. (اختياري) إنشاء Redis للتخزين المؤقت:

1. في نفس مشروع Railway، انقر على **New**
2. اختر **Provision Redis**
3. انسخ رابط الاتصال (`REDIS_URL`)
   ```
   redis://default:password@redis.railway.internal:6379
   ```

---

### 2️⃣ ربط المشروع مع Vercel

1. **افتح لوحة تحكم Vercel**
   - اذهب إلى [vercel.com/dashboard](https://vercel.com/dashboard)

2. **استيراد المشروع**
   - انقر على **Add New... → Project**
   - اختر **Import Git Repository**
   - ابحث عن مستودع `RabitHR` وانقر **Import**

3. **إعدادات المشروع**
   - **Framework Preset**: اختر `Vite`
   - **Root Directory**: اتركه كما هو (./ الجذر)
   - **Build Command**: `pnpm build`
   - **Output Directory**: `dist`
   - **Install Command**: `pnpm install`

---

### 3️⃣ إعداد متغيرات البيئة (Environment Variables)

هذه أهم خطوة! يجب إضافة جميع المتغيرات التالية:

#### متغيرات أساسية (مطلوبة):

```bash
# بيئة التشغيل
NODE_ENV=production

# قاعدة البيانات (من Railway)
DATABASE_URL=mysql://root:password@containers-us-west-xxx.railway.app:3306/railway

# مفتاح JWT (أنشئه باستخدام: openssl rand -base64 32)
JWT_SECRET=ضع-هنا-مفتاح-سري-قوي-32-حرف-على-الأقل

# معلومات التطبيق
VITE_APP_TITLE=رابِط - منصة إدارة الموارد البشرية
VITE_APP_LOGO=/logo.png
VITE_APP_URL=https://your-domain.vercel.app
```

#### متغيرات اختيارية (لتفعيل ميزات إضافية):

```bash
# Redis للتخزين المؤقت (يحسّن الأداء)
REDIS_URL=redis://default:password@redis.railway.internal:6379

# خدمة البريد الإلكتروني (Resend)
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=noreply@yourdomain.com

# AWS S3 لتخزين الملفات
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=rabithr-storage

# OpenAI للميزات الذكية (مولد الخطابات والمساعد الذكي)
OPENAI_API_KEY=sk-your-openai-api-key

# Sentry لتتبع الأخطاء
VITE_SENTRY_DSN=https://your-sentry-dsn
SENTRY_AUTH_TOKEN=your-sentry-token

# بوابات الدفع السعودية
MOYASAR_API_KEY=your_moyasar_key
MOYASAR_SECRET_KEY=your_moyasar_secret
TAP_API_KEY=your_tap_key
TAP_SECRET_KEY=your_tap_secret

# خدمة الرسائل القصيرة (SMS)
SMS_API_KEY=your_sms_api_key
SMS_SENDER_ID=Rabit

# Google Maps للمواقع
GOOGLE_MAPS_API_KEY=your_google_maps_key
```

#### كيفية إضافة المتغيرات في Vercel:

1. في صفحة إعدادات المشروع، انتقل إلى **Settings → Environment Variables**
2. أضف كل متغير على حدة:
   - **Key**: اسم المتغير (مثل `DATABASE_URL`)
   - **Value**: قيمة المتغير
   - **Environments**: اختر `Production`, `Preview`, `Development`
3. انقر **Save** بعد كل متغير

---

### 4️⃣ نشر المشروع

1. **انقر على Deploy**
   - بعد إضافة جميع المتغيرات، انقر **Deploy**
   - سيبدأ Vercel بعملية البناء والنشر

2. **مراقبة عملية البناء**
   - شاهد لوج البناء (Build Logs) للتأكد من عدم وجود أخطاء
   - مدة البناء عادةً: 3-5 دقائق

3. **التحقق من النشر**
   - بعد اكتمال النشر، سيعطيك Vercel رابط المشروع
   - مثال: `https://rabit-hr-xxxxx.vercel.app`

---

### 5️⃣ تشغيل هجرات قاعدة البيانات

بعد النشر الأول، يجب تشغيل هجرات قاعدة البيانات:

#### الطريقة الأولى: من جهازك المحلي

```bash
# 1. انسخ المشروع محلياً (إذا لم يكن منسوخاً)
git clone https://github.com/your-username/RabitHR.git
cd RabitHR

# 2. أنشئ ملف .env وأضف DATABASE_URL من Railway
echo "DATABASE_URL=mysql://root:password@..." > .env

# 3. ثبّت المكتبات
pnpm install

# 4. شغّل الهجرات
pnpm db:push
```

#### الطريقة الثانية: استخدام Vercel CLI

```bash
# 1. ثبّت Vercel CLI
npm i -g vercel

# 2. سجّل دخول
vercel login

# 3. اربط المشروع
vercel link

# 4. شغّل الأمر على Vercel
vercel env pull .env.local
pnpm db:push
```

---

### 6️⃣ ربط نطاق مخصص (اختياري)

لربط نطاقك الخاص (مثل: www.rabit.sa):

1. **في Vercel Dashboard**
   - اذهب إلى **Settings → Domains**
   - انقر **Add Domain**
   - أدخل نطاقك: `rabit.sa` و `www.rabit.sa`

2. **في لوحة تحكم النطاق**
   - أضف سجلات DNS التالية:

   ```
   Type: A
   Name: @
   Value: 76.76.21.21

   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   ```

3. **انتظر التفعيل**
   - قد يستغرق الأمر 24-48 ساعة
   - سيصدر Vercel شهادة SSL تلقائياً

4. **حدّث VITE_APP_URL**
   - في Environment Variables، حدّث:
   ```
   VITE_APP_URL=https://www.rabit.sa
   ```

---

## 🔍 التحقق من النشر

بعد النشر، تأكد من:

### ✅ قائمة التحقق:

- [ ] الصفحة الرئيسية تفتح بدون أخطاء
- [ ] يمكن تسجيل الدخول بحسابات الاختبار
- [ ] قاعدة البيانات متصلة ويمكن استرجاع البيانات
- [ ] الصور والملفات الثابتة تُحمّل بشكل صحيح
- [ ] API endpoints تعمل (جرّب: `/api/trpc/health`)
- [ ] الميزات الذكية تعمل (إذا أضفت OPENAI_API_KEY)

### 🧪 اختبر بحسابات الاختبار:

```
الموظف:
البريد: employee@test.com
كلمة المرور: password123

الشركة:
البريد: company@test.com
كلمة المرور: password123

المستشار:
البريد: consultant@test.com
كلمة المرور: password123

الإدارة:
البريد: admin@test.com
كلمة المرور: password123
```

---

## ⚙️ الإعدادات المتقدمة

### تفعيل ميزات إضافية:

#### 1. التخزين المؤقت مع Redis:

```bash
REDIS_URL=redis://default:password@redis.railway.internal:6379
```

**الفوائد**: تسريع الاستجابات، تقليل الحمل على قاعدة البيانات

#### 2. البريد الإلكتروني:

```bash
RESEND_API_KEY=re_xxxxx
RESEND_FROM_EMAIL=noreply@yourdomain.com
```

**الفوائد**: إرسال إشعارات، استعادة كلمة المرور

#### 3. الذكاء الاصطناعي:

```bash
OPENAI_API_KEY=sk-xxxxx
```

**الفوائد**: مولد الخطابات، المساعد الذكي، اقتراحات ذكية

#### 4. تخزين الملفات:

```bash
AWS_ACCESS_KEY_ID=xxxxx
AWS_SECRET_ACCESS_KEY=xxxxx
AWS_S3_BUCKET=rabithr-storage
```

**الفوائد**: تخزين آمن للوثائق والصور

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: فشل عملية البناء (Build Failed)

**الأعراض**: رسالة خطأ أثناء البناء

```
Error: Cannot find module 'xxx'
```

**الحل**:

1. تأكد من أن `pnpm-lock.yaml` موجود في المشروع
2. تحقق من أن `package.json` صحيح
3. في Vercel Settings، تأكد من:
   - **Install Command**: `pnpm install`
   - **Build Command**: `pnpm build`

---

### المشكلة 2: خطأ في الاتصال بقاعدة البيانات

**الأعراض**:

```
Error: connect ETIMEDOUT
```

**الحل**:

1. تحقق من `DATABASE_URL` في Environment Variables
2. تأكد من أن IP الخاص بـ Vercel مسموح في Railway
3. جرّب الاتصال من جهازك المحلي أولاً
4. في Railway، تأكد من أن القاعدة تقبل اتصالات خارجية

---

### المشكلة 3: الصفحات لا تُحمّل (404 أو 500)

**الأعراض**: صفحات فارغة أو أخطاء 404

**الحل**:

1. تحقق من أن `vercel.json` موجود في الجذر
2. تأكد من أن rewrites صحيحة:

```json
"rewrites": [
  {
    "source": "/api/trpc/:path*",
    "destination": "/server/_core/index.ts"
  }
]
```

3. تحقق من Function Logs في Vercel Dashboard

---

### المشكلة 4: الهجرات لم تُنفّذ

**الأعراض**: جداول قاعدة البيانات غير موجودة

**الحل**:

```bash
# من جهازك المحلي
pnpm db:push

# أو استخدم SQL مباشرةً في Railway Dashboard
```

---

### المشكلة 5: متغيرات البيئة لا تعمل

**الأعراض**: الميزات لا تعمل رغم إضافة المتغيرات

**الحل**:

1. تأكد من أن المتغيرات مفعّلة لـ **Production**
2. أعد نشر المشروع بعد إضافة المتغيرات
3. المتغيرات التي تبدأ بـ `VITE_` يجب أن تُضاف قبل البناء
4. في Vercel Dashboard: **Deployments → Redeploy**

---

## 📊 المراقبة والصيانة

### مراقبة الأداء:

1. **Vercel Analytics**
   - تلقائية ومجانية
   - تتبع الزوار والأداء

2. **Function Logs**
   - في Dashboard → Functions
   - راقب أخطاء API

3. **Sentry للأخطاء**
   - أضف `VITE_SENTRY_DSN`
   - راقب الأخطاء في الوقت الفعلي

### التحديثات التلقائية:

Vercel سينشر تلقائياً عند:

- Push إلى branch `main`
- Pull Request جديد (للمعاينة)

---

## 💡 نصائح للأداء الأفضل

### 1. استخدم Redis:

```bash
REDIS_URL=redis://...
```

يُحسّن أداء الاستجابات بنسبة 70%

### 2. فعّل Compression:

في `vercel.json`:

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Encoding",
          "value": "gzip"
        }
      ]
    }
  ]
}
```

### 3. استخدم CDN للملفات:

- ارفع الصور على S3 أو Cloudinary
- استخدم `BUILT_IN_FORGE_API_URL` للملفات الكبيرة

### 4. راقب الاستخدام:

- Vercel المجاني: 100GB Bandwidth/شهر
- Database: راقب الحجم في Railway

---

## 🎯 الخطوات التالية

بعد النشر بنجاح:

1. **✅ أنشئ حساب Admin**
   - سجّل بحساب جديد
   - أو استخدم `admin@test.com`

2. **✅ خصّص الإعدادات**
   - شعار الشركة
   - معلومات الاتصال
   - إعدادات النظام

3. **✅ أضف المستخدمين**
   - دعوة الموظفين
   - إعداد الصلاحيات

4. **✅ راجع الأمان**
   - غيّر كلمات المرور الافتراضية
   - فعّل المصادقة الثنائية (2FA)
   - راجع الصلاحيات

---

## 📞 الدعم والمساعدة

### واجهت مشكلة؟

1. **راجع Function Logs** في Vercel
2. **تحقق من Railway Logs** لقاعدة البيانات
3. **اقرأ الوثائق**:
   - [Vercel Docs](https://vercel.com/docs)
   - [Railway Docs](https://docs.railway.app)

### تواصل معنا:

- 📧 **البريد**: info@rbithr.com
- 📱 **الهاتف**: 0570700355
- 🌐 **الموقع**: https://rabit.sa

---

## 📚 مراجع مفيدة

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel CLI](https://vercel.com/docs/cli)
- [Railway Documentation](https://docs.railway.app)
- [MySQL on Railway](https://docs.railway.app/databases/mysql)
- [Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

## ✅ قائمة التحقق النهائية

قبل الإطلاق الرسمي، تأكد من:

- [ ] قاعدة البيانات متصلة وتعمل
- [ ] جميع متغيرات البيئة الأساسية مضافة
- [ ] الهجرات تم تنفيذها بنجاح
- [ ] حسابات الاختبار تعمل
- [ ] جميع الصفحات تُفتح بدون أخطاء
- [ ] API endpoints تستجيب بشكل صحيح
- [ ] الصور والملفات الثابتة تُحمّل
- [ ] Redis مفعّل (للأداء الأفضل)
- [ ] SSL شهادة نشطة (تلقائي من Vercel)
- [ ] النطاق المخصص مربوط (إن وُجد)
- [ ] كلمات المرور الافتراضية تم تغييرها
- [ ] النسخ الاحتياطي مُجدول
- [ ] المراقبة والتنبيهات مفعّلة

---

<div align="center">

**🎉 مبروك! منصة رابِط الآن على Vercel 🎉**

**صُنع بـ ❤️ في السعودية 🇸🇦**

</div>
