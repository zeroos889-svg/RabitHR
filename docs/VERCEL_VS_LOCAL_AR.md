# 🔄 الفرق بين التشغيل المحلي والنشر على Vercel

## 📊 جدول المقارنة

| الميزة              | التشغيل المحلي       | النشر على Vercel             |
| ------------------- | -------------------- | ---------------------------- |
| **السرعة**          | متوسطة               | سريع جداً (CDN عالمي)        |
| **التكلفة**         | مجاناً               | مجاناً (حتى 100GB/شهر)       |
| **قاعدة البيانات**  | محلية (MySQL/SQLite) | خارجية (Railway/PlanetScale) |
| **الوصول**          | localhost فقط        | من أي مكان عبر الإنترنت      |
| **SSL/HTTPS**       | غير متوفر            | تلقائي ومجاني                |
| **الصيانة**         | يدوية                | تلقائية                      |
| **التحديثات**       | يدوية                | تلقائية عند Push             |
| **النسخ الاحتياطي** | يدوي                 | آلي                          |
| **المراقبة**        | يدوية                | مدمجة (Analytics)            |

---

## 🏠 التشغيل المحلي (Development)

### المتطلبات:

- Node.js 18+
- pnpm
- MySQL محلي

### الأوامر:

```bash
pnpm install
pnpm dev
# → http://localhost:3000
```

### المميزات:

- ✅ تطوير سريع
- ✅ اختبار فوري
- ✅ Hot Reload
- ✅ لا يحتاج إنترنت (بعد التثبيت)

### العيوب:

- ❌ غير متاح للآخرين
- ❌ لا يوجد HTTPS
- ❌ يجب أن يظل الجهاز مشغلاً

### متى تستخدمه:

- 🔧 أثناء التطوير
- 🧪 اختبار ميزات جديدة
- 🐛 تصحيح الأخطاء

---

## ☁️ النشر على Vercel (Production)

### المتطلبات:

- حساب Vercel
- حساب GitHub
- قاعدة بيانات خارجية (Railway)

### الإعداد:

```bash
# مرة واحدة فقط
1. ربط GitHub مع Vercel
2. إضافة متغيرات البيئة
3. Deploy
```

### المميزات:

- ✅ متاح 24/7
- ✅ SSL تلقائي
- ✅ CDN عالمي
- ✅ نشر تلقائي
- ✅ معاينة فورية للـ PRs
- ✅ مراقبة مدمجة
- ✅ نطاق مخصص
- ✅ نسخ احتياطية

### العيوب:

- ❌ يحتاج إنترنت
- ❌ تكلفة محتملة (بعد 100GB)
- ❌ قاعدة بيانات خارجية

### متى تستخدمه:

- 🚀 إطلاق رسمي
- 👥 مشاركة مع العملاء
- 📊 بيئة إنتاج حقيقية
- 🔐 بيانات حقيقية

---

## 🎯 السيناريوهات الشائعة

### سيناريو 1: أنا مطور ألعب بالكود

```bash
✅ استخدم: التشغيل المحلي
📍 الأمر: pnpm dev
```

### سيناريو 2: أريد أن يراه العميل

```bash
✅ استخدم: Vercel
📍 الطريقة: Deploy to Production
```

### سيناريو 3: أريد اختبار ميزة جديدة مع الفريق

```bash
✅ استخدم: Vercel Preview
📍 الطريقة: افتح PR → Vercel ينشئ معاينة تلقائياً
```

### سيناريو 4: الإطلاق الرسمي

```bash
✅ استخدم: Vercel Production
📍 مع: نطاق مخصص + SSL + مراقبة
```

---

## 🔄 سير العمل الموصى به (Workflow)

### 1. **التطوير المحلي**

```bash
git checkout -b feature/new-feature
pnpm dev
# طور وجرب محلياً
```

### 2. **Push للـ Branch**

```bash
git add .
git commit -m "إضافة ميزة جديدة"
git push origin feature/new-feature
```

### 3. **معاينة تلقائية على Vercel**

```bash
# Vercel ينشئ رابط معاينة تلقائياً
→ https://rabit-hr-git-feature-new-feature.vercel.app
# شارك الرابط مع الفريق للمراجعة
```

### 4. **دمج في Main**

```bash
# بعد الموافقة
git checkout main
git merge feature/new-feature
git push
```

### 5. **نشر تلقائي على Production**

```bash
# Vercel ينشر تلقائياً على:
→ https://rabit-hr.vercel.app
# أو نطاقك المخصص:
→ https://www.rabit.sa
```

---

## ⚙️ متغيرات البيئة

### محلياً:

```bash
# ملف .env
DATABASE_URL=mysql://root:password@localhost:3306/rabithr
REDIS_URL=redis://localhost:6379
VITE_APP_URL=http://localhost:3000
```

### على Vercel:

```bash
# في Vercel Dashboard → Settings → Environment Variables
DATABASE_URL=mysql://...railway.app:3306/railway
REDIS_URL=redis://...railway.internal:6379
VITE_APP_URL=https://www.rabit.sa
```

---

## 🚨 ملاحظات مهمة

### ⚠️ لا تخلط البيئات!

```bash
# ❌ خطأ: استخدام قاعدة بيانات الإنتاج محلياً
DATABASE_URL=mysql://...production-db...

# ✅ صحيح: قاعدة بيانات محلية للتطوير
DATABASE_URL=mysql://root:password@localhost:3306/rabithr_dev
```

### ⚠️ المتغيرات التي تبدأ بـ VITE\_

```bash
# هذه المتغيرات يجب إضافتها قبل البناء
VITE_APP_TITLE=...
VITE_APP_URL=...

# إذا غيّرتها على Vercel:
1. حدّث المتغير
2. أعد النشر (Redeploy)
```

### ⚠️ الملفات السرية

```bash
# ❌ لا تضع في .env قيم الإنتاج
# ✅ ضعها في Vercel Dashboard فقط

# ملف .env للتطوير فقط
# ملف .env.example للتوثيق فقط
```

---

## 📈 نصائح الأداء

### للتطوير المحلي:

```bash
# استخدم قاعدة بيانات صغيرة
# استخدم بيانات تجريبية
# فعّل Hot Reload
```

### للإنتاج على Vercel:

```bash
# فعّل Redis
REDIS_URL=...

# استخدم CDN للملفات
AWS_S3_BUCKET=...

# راقب الأداء
VITE_SENTRY_DSN=...
```

---

## 🎓 الخلاصة

| الاستخدام      | البيئة المناسبة               |
| -------------- | ----------------------------- |
| تطوير          | محلي                          |
| اختبار         | محلي + Vercel Preview         |
| مراجعة الفريق  | Vercel Preview                |
| تجربة العميل   | Vercel Production             |
| الإطلاق الرسمي | Vercel Production + نطاق مخصص |

---

**💡 نصيحة ذهبية:**

> طوّر محلياً، جرّب على Preview، انشر على Production

---

**📚 المراجع:**

- [VERCEL_SETUP.md](../VERCEL_SETUP.md) - دليل النشر الكامل
- [VERCEL_QUICKSTART_AR.md](../VERCEL_QUICKSTART_AR.md) - البدء السريع
- [.env.example](../.env.example) - جميع المتغيرات
