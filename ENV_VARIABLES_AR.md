# 🔐 دليل المتغيرات البيئية - منصة رابِط للموارد البشرية

## 📋 جدول المحتويات
- [نظرة عامة](#نظرة-عامة)
- [الإعداد السريع](#الإعداد-السريع)
- [المتغيرات المطلوبة](#المتغيرات-المطلوبة)
- [المتغيرات الاختيارية](#المتغيرات-الاختيارية)
- [التكوين حسب البيئة](#التكوين-حسب-البيئة)

---

## 🌟 نظرة عامة

يستخدم تطبيق رابِط للموارد البشرية **44 متغير بيئي** لتهيئة الخدمات والميزات المختلفة. تم تقسيمهم إلى متغيرات **مطلوبة** و**اختيارية** لتسهيل الإعداد.

---

## ⚡ الإعداد السريع

### الخطوات الأساسية:
```bash
# 1. نسخ ملف المثال
cp .env.example .env

# 2. تعديل ملف .env بالقيم المناسبة
nano .env

# 3. إنشاء مفتاح JWT آمن
openssl rand -base64 32

# 4. تشغيل التطبيق
pnpm install
pnpm dev
```

---

## ✅ المتغيرات المطلوبة

هذه المتغيرات **ضرورية** لتشغيل التطبيق:

### 🗄️ قاعدة البيانات
| المتغير | الوصف | مثال |
|---------|-------|------|
| `DATABASE_URL` | عنوان الاتصال بقاعدة بيانات MySQL | `mysql://user:pass@host:3306/db` |

**خيارات قاعدة البيانات:**
1. **Railway MySQL**: احصل على الرابط من لوحة تحكم Railway
2. **TiDB Cloud**: استخدم رابط الاتصال من TiDB Dashboard
3. **MySQL محلي**: `mysql://root:password@localhost:3306/rabithr_dev`

### 🔒 الأمان والمصادقة
| المتغير | الوصف | كيفية الإنشاء |
|---------|-------|---------------|
| `JWT_SECRET` | مفتاح سري لتشفير JWT (32 حرف على الأقل) | `openssl rand -base64 32` |
| `SESSION_SECRET` | مفتاح سري للجلسات | `openssl rand -base64 32` |
| `ADMIN_EMAIL` | البريد الإلكتروني للمدير الأول | `admin@rabit.sa` |
| `ADMIN_PASSWORD` | كلمة المرور للمدير الأول | كلمة مرور قوية |

### 🌐 عناوين التطبيق
| المتغير | الوصف | القيمة الافتراضية |
|---------|-------|------------------|
| `NODE_ENV` | بيئة التشغيل | `development` أو `production` |
| `PORT` | منفذ الخادم | `3000` |

---

## 🎨 المتغيرات الاختيارية

### 🖥️ واجهة المستخدم
| المتغير | الوصف | القيمة الافتراضية |
|---------|-------|------------------|
| `VITE_APP_URL` | رابط الواجهة الأمامية | `http://localhost:3000` |
| `VITE_API_URL` | رابط الواجهة الخلفية API | `http://localhost:3000` |
| `VITE_APP_TITLE` | عنوان التطبيق | `رابِط - منصة إدارة الموارد البشرية` |
| `VITE_APP_LOGO` | شعار التطبيق | `/logo.png` |

### 📧 خدمة البريد الإلكتروني

#### Resend (موصى به)
| المتغير | الوصف | كيفية الحصول عليه |
|---------|-------|------------------|
| `RESEND_API_KEY` | مفتاح API من Resend | [resend.com/api-keys](https://resend.com/api-keys) |
| `RESEND_FROM_EMAIL` | البريد المرسل | `noreply@rabit.sa` |

#### SMTP (بديل)
| المتغير | الوصف |
|---------|-------|
| `SMTP_HOST` | خادم SMTP |
| `SMTP_PORT` | منفذ SMTP (587 للتشفير TLS) |
| `SMTP_USER` | اسم المستخدم |
| `SMTP_PASSWORD` | كلمة المرور |
| `SMTP_FROM` | البريد المرسل |

### 💬 خدمة الرسائل النصية (SMS)

| المتغير | الوصف | الخدمة |
|---------|-------|--------|
| `SMS_API_KEY` | مفتاح API عام | - |
| `SMS_SENDER_ID` | معرف المرسل | `Rabit` |
| `TWILIO_PHONE_NUMBER` | رقم Twilio | Twilio |
| `TWILIO_AUTH_TOKEN` | رمز المصادقة | Twilio |
| `UNIFONIC_APP_SID` | معرف التطبيق | Unifonic (السعودية) |

### 💳 بوابات الدفع

#### Moyasar
| المتغير | الوصف |
|---------|-------|
| `MOYASAR_API_KEY` | مفتاح API |
| `MOYASAR_SECRET_KEY` | المفتاح السري |
| `MOYASAR_WEBHOOK_SECRET` | مفتاح Webhook |

#### Tap Payments
| المتغير | الوصف |
|---------|-------|
| `TAP_API_KEY` | مفتاح API |
| `TAP_SECRET_KEY` | المفتاح السري |

### ☁️ التخزين السحابي (AWS S3)
| المتغير | الوصف | القيمة الافتراضية |
|---------|-------|------------------|
| `AWS_ACCESS_KEY_ID` | معرف الوصول | - |
| `AWS_SECRET_ACCESS_KEY` | المفتاح السري | - |
| `AWS_REGION` | المنطقة | `us-east-1` |
| `AWS_S3_BUCKET` | اسم Bucket | `rabithr-storage` |

### 🗺️ خرائط جوجل
| المتغير | الوصف | كيفية الحصول عليه |
|---------|-------|------------------|
| `GOOGLE_MAPS_API_KEY` | مفتاح API | [Google Cloud Console](https://console.cloud.google.com/) |

### 🤖 الذكاء الاصطناعي
| المتغير | الوصف | كيفية الحصول عليه |
|---------|-------|------------------|
| `OPENAI_API_KEY` | مفتاح OpenAI API | [platform.openai.com](https://platform.openai.com/api-keys) |

### 🔧 Forge Storage API
| المتغير | الوصف | الاستخدام |
|---------|-------|----------|
| `BUILT_IN_FORGE_API_URL` | رابط API للخادم | عمليات الخادم |
| `BUILT_IN_FORGE_API_KEY` | مفتاح API للخادم | عمليات الخادم |
| `VITE_FRONTEND_FORGE_API_URL` | رابط API للواجهة | عمليات الخرائط |
| `VITE_FRONTEND_FORGE_API_KEY` | مفتاح API للواجهة | عمليات الخرائط |

### 📊 التحليلات والمراقبة

#### Analytics
| المتغير | الوصف |
|---------|-------|
| `VITE_ANALYTICS_ENDPOINT` | نقطة نهاية التحليلات |
| `VITE_ANALYTICS_WEBSITE_ID` | معرف الموقع |

#### Sentry (تتبع الأخطاء)
| المتغير | الوصف | كيفية الحصول عليه |
|---------|-------|------------------|
| `VITE_SENTRY_DSN` | رابط Sentry | [sentry.io](https://sentry.io/settings/projects/) |
| `SENTRY_AUTH_TOKEN` | رمز المصادقة | Sentry Settings |
| `VITE_SENTRY_DEBUG` | وضع التصحيح | `false` (افتراضي) |

### 🚀 Redis (التخزين المؤقت)
| المتغير | الوصف | القيمة الافتراضية |
|---------|-------|------------------|
| `REDIS_URL` | رابط اتصال Redis | `redis://localhost:6379` |

**فوائد Redis:**
- تحسين الأداء
- تخزين الجلسات
- التخزين المؤقت للبيانات

### 🔐 OAuth (اختياري)
| المتغير | الوصف |
|---------|-------|
| `VITE_OAUTH_PORTAL_URL` | رابط بوابة OAuth |
| `VITE_APP_ID` | معرف التطبيق |

### 🐳 Docker (للتطوير المحلي)
| المتغير | الوصف | القيمة الافتراضية |
|---------|-------|------------------|
| `MYSQL_ROOT_PASSWORD` | كلمة مرور MySQL في Docker | `rootpassword` |

---

## 🎯 التكوين حسب البيئة

### بيئة التطوير (Development)
```env
NODE_ENV=development
DATABASE_URL=mysql://root:password@localhost:3306/rabithr_dev
JWT_SECRET=your-dev-secret-min-32-chars
SESSION_SECRET=your-dev-session-secret
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=DevPassword123!
VITE_APP_URL=http://localhost:3000
VITE_API_URL=http://localhost:3000
PORT=3000
REDIS_URL=redis://localhost:6379
```

### بيئة الإنتاج (Production)

#### Frontend (Vercel)
```env
NODE_ENV=production
VITE_APP_URL=https://rabit-hr.vercel.app
VITE_API_URL=https://your-backend.railway.app
VITE_APP_TITLE=رابِط - منصة إدارة الموارد البشرية
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
```

#### Backend (Railway)
```env
NODE_ENV=production
DATABASE_URL=mysql://user:password@host:port/database
JWT_SECRET=your-super-secure-production-secret-min-32-chars
SESSION_SECRET=your-super-secure-session-secret
SESSION_MAX_AGE=604800000
ADMIN_EMAIL=admin@rabit.sa
ADMIN_PASSWORD=SecureProductionPassword123!
PORT=3000
REDIS_URL=redis://user:password@host:port

# خدمات اختيارية
RESEND_API_KEY=re_your_production_key
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
MOYASAR_API_KEY=your_moyasar_key
OPENAI_API_KEY=sk-your_openai_key
```

### Docker Compose
```env
NODE_ENV=production
DATABASE_URL=mysql://root:rootpassword@db:3306/rabithr
MYSQL_ROOT_PASSWORD=rootpassword
JWT_SECRET=your-docker-secret-min-32-chars
SESSION_SECRET=your-docker-session-secret
REDIS_URL=redis://redis:6379
```

---

## 🔒 أفضل الممارسات الأمنية

### ✅ افعل:
- استخدم مفاتير سرية طويلة وعشوائية (32+ حرف)
- غير كلمة مرور المدير الافتراضية فوراً
- استخدم HTTPS في بيئة الإنتاج
- احفظ ملف `.env` في `.gitignore`
- استخدم خدمات إدارة الأسرار (Vercel Environment Variables، Railway Environment Variables)
- راجع وحدث المفاتيح السرية بشكل دوري

### ❌ لا تفعل:
- لا تشارك ملف `.env` مع الآخرين
- لا ترفع ملف `.env` إلى GitHub أو أي مستودع عام
- لا تستخدم قيم افتراضية في الإنتاج
- لا تضع أسرار في الكود المصدري
- لا تعيد استخدام نفس المفاتيح بين البيئات المختلفة

---

## 📝 ملاحظات مهمة

1. **المتغيرات المطلوبة**: فقط `DATABASE_URL` و `JWT_SECRET` مطلوبان لتشغيل التطبيق الأساسي
2. **المتغيرات الاختيارية**: تفعل ميزات إضافية (الدفع، الرسائل، التخزين السحابي، إلخ)
3. **متغيرات VITE_***: متغيرات الواجهة الأمامية يجب أن تبدأ بـ `VITE_`
4. **البيئات المتعددة**: استخدم ملفات `.env.development` و `.env.production` للفصل بين البيئات

---

## 🆘 المساعدة والدعم

إذا واجهت أي مشاكل:
1. تحقق من أن جميع المتغيرات المطلوبة موجودة
2. تأكد من صحة صيغة رابط قاعدة البيانات
3. راجع سجلات الأخطاء في التطبيق
4. تحقق من صلاحيات الاتصال بالخدمات الخارجية

---

## 📚 موارد إضافية

- [دليل التثبيت (INSTALLATION.md)](./INSTALLATION.md)
- [دليل النشر (DEPLOYMENT_GUIDE_FULL.md)](./DEPLOYMENT_GUIDE_FULL.md)
- [الأمان (SECURITY.md)](./SECURITY.md)
- [البداية السريعة (QUICK_START.md)](./QUICK_START.md)

---

**آخر تحديث:** 2025-11-05  
**الإصدار:** 1.0.0
