# 🎯 دليل النشر على Vercel مع قاعدة البيانات الجاهزة

## ✅ لديك الآن قاعدة بيانات جاهزة!

لديك **خياران** لقاعدة البيانات، كلاهما جاهز:

**الخيار 1: Railway MySQL** (موصى به للبداية)

```
mysql://root:<RAILWAY_PASSWORD>@containers-us-west-xxx.railway.app:3306/railway
```

**الخيار 2: TiDB Cloud** (موصى به للمشاريع الكبيرة)

```
mysql://<USERNAME>.root:<PASSWORD>@gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/test
```

⚠️ **ملاحظة**: للخيار 2، استبدل `<PASSWORD>` بكلمة المرور من TiDB Dashboard

📚 **للمقارنة التفصيلية**: اقرأ [DATABASE_OPTIONS.md](./DATABASE_OPTIONS.md)

---

## 🚀 خطوات النشر (10 دقائق)

### الخطوة 1: إنشاء مفتاح JWT

افتح Terminal (موجه الأوامر) واكتب:

```bash
openssl rand -base64 32
```

ستحصل على شيء مثل:

```
a9f3k2m5n8p1q4r7s0t3v6w9x2y5z8A1
```

احفظ هذا المفتاح، ستحتاجه في الخطوة التالية.

---

### الخطوة 2: النشر على Vercel

#### أ. افتح Vercel:

1. اذهب إلى: [vercel.com/new](https://vercel.com/new)
2. سجّل دخول (أو أنشئ حساب جديد)

#### ب. استيراد المشروع:

1. انقر **Import Git Repository**
2. اختر مستودع **RabitHR**
3. انقر **Import**

#### ج. إعدادات المشروع:

- **Framework Preset**: اختر **Vite**
- **Root Directory**: اتركه `./`
- **Build Command**: سيتم اختياره تلقائياً (`pnpm build`)
- **Output Directory**: سيتم اختياره تلقائياً (`dist`)
- **Install Command**: سيتم اختياره تلقائياً (`pnpm install`)

---

### الخطوة 3: إضافة متغيرات البيئة

في قسم **Environment Variables**، أضف المتغيرات التالية **واحدة تلو الأخرى**:

#### المتغيرات الأساسية (مطلوبة):

```bash
# 1. بيئة التشغيل
NODE_ENV
production

# 2. قاعدة البيانات (استخدم الرابط الذي لديك)
DATABASE_URL
mysql://root:<RAILWAY_PASSWORD>@containers-us-west-xxx.railway.app:3306/railway

# 3. مفتاح JWT (من الخطوة 1)
JWT_SECRET
[ضع هنا المفتاح الذي أنشأته في الخطوة 1]

# 4. مفتاح الجلسة (يمكن أن يكون نفس JWT_SECRET)
SESSION_SECRET
[ضع هنا المفتاح الذي أنشأته في الخطوة 1]

# 5. عنوان التطبيق
VITE_APP_TITLE
رابِط - منصة إدارة الموارد البشرية

# 6. شعار التطبيق
VITE_APP_LOGO
/logo.png

# 7. رابط التطبيق (سنحدثه لاحقاً)
VITE_APP_URL
https://your-app-name.vercel.app
```

#### كيفية إضافة كل متغير:

1. في خانة **Key**: اكتب اسم المتغير (مثل: `DATABASE_URL`)
2. في خانة **Value**: اكتب القيمة
3. في **Environment**: اختر `Production`, `Preview`, `Development` (اختر الكل)
4. انقر **Add**
5. كرر للمتغير التالي

---

### الخطوة 4: النشر

1. بعد إضافة جميع المتغيرات، انقر **Deploy**
2. انتظر 3-5 دقائق حتى يكتمل البناء
3. ستظهر رسالة نجاح مع رابط المشروع

---

### الخطوة 5: تحديث VITE_APP_URL

بعد نجاح النشر:

1. انسخ رابط المشروع (مثل: `https://rabit-hr-abc123.vercel.app`)
2. اذهب إلى **Settings → Environment Variables**
3. ابحث عن `VITE_APP_URL`
4. انقر **Edit**
5. غيّر القيمة إلى الرابط الجديد
6. **احفظ**
7. اذهب إلى **Deployments**
8. انقر على النشر الأخير → انقر **⋯** → اختر **Redeploy**

---

### الخطوة 6: تشغيل هجرات قاعدة البيانات

الآن يجب إنشاء الجداول في قاعدة البيانات:

#### الطريقة 1: من جهازك (الأسهل)

```bash
# 1. افتح Terminal
# 2. اذهب إلى مجلد المشروع
cd RabitHR

# 3. أنشئ ملف .env
nano .env
# أو
notepad .env

# 4. أضف السطر التالي (اختر أحد الخيارين):

# الخيار 1: Railway (جاهز - موصى به)
DATABASE_URL=mysql://root:<RAILWAY_PASSWORD>@containers-us-west-xxx.railway.app:3306/railway

# الخيار 2: TiDB (استبدل <PASSWORD> بكلمة المرور من TiDB Dashboard)
# DATABASE_URL=mysql://<USERNAME>.root:<PASSWORD>@gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/test

# 5. احفظ الملف (Ctrl+X ثم Y ثم Enter في nano)

# 6. ثبّت المكتبات
pnpm install

# 7. شغّل الهجرات
pnpm db:push
```

ستظهر رسائل تؤكد إنشاء الجداول بنجاح.

---

## ✅ اختبار المشروع

### 1. افتح رابط المشروع

افتح الرابط الذي حصلت عليه من Vercel (مثل: `https://rabit-hr-abc123.vercel.app`)

### 2. جرّب تسجيل الدخول

استخدم أحد الحسابات التجريبية:

```
موظف:
employee@test.com
password123

شركة:
company@test.com
password123

مستشار:
consultant@test.com
password123

إدارة:
admin@test.com
password123
```

### 3. تحقق من الميزات

- ✅ الصفحة الرئيسية تفتح
- ✅ يمكن تسجيل الدخول
- ✅ البيانات تُحفظ وتُسترجع
- ✅ الصور تظهر بشكل صحيح

---

## 🎨 (اختياري) إضافة ميزات إضافية

### 1. Redis للتخزين المؤقت (يُحسّن الأداء)

في Railway، أضف Redis:

1. افتح مشروعك في Railway
2. انقر **New** → **Redis**
3. انسخ `REDIS_URL`
4. في Vercel، أضف متغير جديد:

```
REDIS_URL
redis://default:password@redis.railway.internal:6379
```

5. أعد النشر

### 2. OpenAI للميزات الذكية

للحصول على مولد الخطابات والمساعد الذكي:

1. سجّل على [platform.openai.com](https://platform.openai.com)
2. أنشئ API Key
3. في Vercel، أضف:

```
OPENAI_API_KEY
sk-your-api-key-here
```

4. أعد النشر

### 3. خدمة البريد الإلكتروني (Resend)

لإرسال إشعارات بالبريد:

1. سجّل على [resend.com](https://resend.com)
2. أنشئ API Key
3. في Vercel، أضف:

```
RESEND_API_KEY
re_your_api_key

RESEND_FROM_EMAIL
noreply@yourdomain.com
```

4. أعد النشر

---

## 🌐 ربط نطاق مخصص (اختياري)

إذا كان لديك نطاق خاص (مثل: rabit.sa):

### في Vercel:

1. اذهب إلى **Settings → Domains**
2. انقر **Add**
3. أدخل نطاقك: `rabit.sa` أو `www.rabit.sa`
4. انقر **Add**

### في لوحة تحكم النطاق:

أضف هذه السجلات (DNS Records):

```
Type: A
Name: @
Value: 76.76.21.21
TTL: Auto

Type: CNAME
Name: www
Value: cname.vercel-dns.com
TTL: Auto
```

### انتظر:

- قد يستغرق 24-48 ساعة
- Vercel سيصدر شهادة SSL تلقائياً
- بعد التفعيل، حدّث `VITE_APP_URL` إلى نطاقك الجديد

---

## 🐛 حل المشاكل الشائعة

### مشكلة: Build Failed (فشل البناء)

**الحل:**

1. تحقق من أن `package.json` موجود
2. تأكد من اختيار Framework: **Vite**
3. تحقق من Function Logs في Vercel

### مشكلة: Cannot connect to database

**الحل:**

1. تحقق من `DATABASE_URL` في Environment Variables
2. تأكد من نسخ الرابط بالكامل بدون مسافات
3. جرّب الاتصال بقاعدة البيانات من Railway Dashboard

### مشكلة: الصفحات تعطي 404

**الحل:**

1. تأكد من أن `vercel.json` موجود في جذر المشروع
2. أعد النشر من Deployments → Redeploy

### مشكلة: الهجرات لم تُنفّذ (الجداول غير موجودة)

**الحل:**

```bash
# من جهازك
pnpm db:push
```

---

## 📊 ملخص ما فعلناه

✅ **أنشأنا قاعدة بيانات** على Railway  
✅ **نشرنا المشروع** على Vercel  
✅ **أضفنا متغيرات البيئة** المطلوبة  
✅ **شغّلنا الهجرات** لإنشاء الجداول  
✅ **اختبرنا المشروع** والتأكد من عمله

---

## 🎯 الخطوات التالية

1. **غيّر كلمات المرور** الافتراضية
2. **أنشئ حساب إدارة** حقيقي
3. **خصّص الإعدادات** (شعار، ألوان، إلخ)
4. **أضف مستخدمين** حقيقيين
5. **فعّل الميزات الإضافية** (Redis, OpenAI, إلخ)
6. **اربط نطاقك** المخصص

---

## 📞 تواصل معنا

إذا واجهت أي مشكلة:

- 📧 **البريد**: info@rbithr.com
- 📱 **الهاتف**: 0570700355
- 🌐 **الموقع**: https://rabit.sa

---

## 📚 مراجع مفيدة

- [VERCEL_SETUP.md](./VERCEL_SETUP.md) - الدليل الكامل المفصّل
- [VERCEL_QUICKSTART_AR.md](./VERCEL_QUICKSTART_AR.md) - البدء السريع
- [.env.example](./.env.example) - جميع المتغيرات المتاحة
- [TEST_USERS.md](./TEST_USERS.md) - حسابات الاختبار

---

<div align="center">

**🎉 مبروك! مشروعك الآن مباشر على الإنترنت! 🎉**

**صُنع بـ ❤️ في السعودية 🇸🇦**

</div>
