# ==============================================
# Environment Configuration for RabitHR Platform
# ==============================================

# ------------------
# Node Environment
# ------------------
NODE_ENV=development

# ------------------
# Database (PostgreSQL - موصى به)
# ------------------
## استخدم Postgres محلياً أو مستضافاً (Neon/Supabase/Railway Postgres)

# الخيار الموصى به للتطوير المحلي (يتوافق مع prisma/schema.prisma)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rabit_hq?schema=public

# أمثلة مستضافة (حدّث القيم الفعلية):
# Neon:      postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
# Supabase:  postgresql://USER:PASSWORD@HOST:PORT/postgres
# Railway:   postgresql://USER:PASSWORD@HOST:PORT/DB

# (Legacy) أمثلة MySQL قديمة — غير مستخدمة حالياً مع Prisma في هذا المشروع:
# DATABASE_URL=mysql://root:<PASSWORD>@host:3306/rabithr

# ------------------
# Authentication & Security
# ------------------
# Secret key for JWT tokens (generate using: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Session Configuration
SESSION_SECRET=your-session-secret-key-change-this
SESSION_MAX_AGE=604800000

# Admin Configuration (first admin user)
ADMIN_EMAIL=admin@rabit.sa
ADMIN_PASSWORD=change-this-secure-password

# ------------------
# Application URL
# ------------------
# Frontend URL (Vercel deployment)
VITE_APP_URL=http://localhost:3000
VITE_APP_TITLE=رابِط - منصة إدارة الموارد البشرية
VITE_APP_LOGO=/logo.png

# Backend API URL (Railway deployment)
# For local development: http://localhost:3000
# For production: https://your-railway-backend.railway.app
VITE_API_URL=http://localhost:3000

# OAuth Portal Configuration (Optional)
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=

# ------------------
# Analytics (Optional)
# ------------------
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# ------------------
# AWS S3 Storage (Optional)
# ------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=rabithr-storage
# S3 bucket name (alternative variable name used in some places)
AWS_S3_BUCKET_NAME=rabithr-storage

# ------------------
# Forge Storage API (Optional)
# ------------------
# Backend Forge API (for server-side operations)
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# Frontend Forge API (for client-side map operations)
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=https://forge.butterfly-effect.dev

# ------------------
# Payment Gateways (Optional)
# ------------------
# Moyasar Payment Gateway
MOYASAR_API_KEY=
MOYASAR_SECRET_KEY=
MOYASAR_WEBHOOK_SECRET=

# Tap Payments
TAP_API_KEY=
TAP_SECRET_KEY=

# ------------------
# Email Service (Resend)
# ------------------
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=noreply@rabit.sa

# Legacy SMTP (Optional - for fallback)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@rabit.sa

# ------------------
# SMS Service (Optional)
# ------------------
SMS_API_KEY=
SMS_SENDER_ID=Rabit

# Twilio SMS (Optional)
TWILIO_PHONE_NUMBER=
TWILIO_AUTH_TOKEN=

# Unifonic SMS (Optional - Saudi Arabia)
UNIFONIC_APP_SID=

# ------------------
# Google Maps API (Optional)
# ------------------
GOOGLE_MAPS_API_KEY=

# ------------------
# Port Configuration
# ------------------
PORT=3000

# ------------------
# Error Tracking (Sentry)
# ------------------
# Get your DSN from: https://sentry.io/settings/projects/
VITE_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
VITE_SENTRY_DEBUG=false

# ------------------
# OpenAI API (for AI features)
# ------------------
OPENAI_API_KEY=

# ------------------
# Redis Cache (Optional - for better performance)
# ------------------
# Format: redis://username:password@host:port
# For Railway Redis: Get from Redis service dashboard
# For local development: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# ------------------
# Docker Configuration (Optional - for local Docker setup)
# ------------------
# MySQL root password for Docker container
MYSQL_ROOT_PASSWORD=rootpassword

# ==============================================
# Setup Instructions:
# ==============================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update DATABASE_URL with your Railway MySQL credentials
# 3. Generate JWT_SECRET: openssl rand -base64 32
# 4. Set ADMIN_EMAIL and ADMIN_PASSWORD for first admin user
# 5. (Optional) Set REDIS_URL for Redis caching
# 6. Fill in optional services as needed
# 7. Never commit .env file to version control
# ==============================================
